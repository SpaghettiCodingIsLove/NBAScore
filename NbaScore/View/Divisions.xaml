<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:NbaScore.ViewModel"
             x:Class="NbaScore.View.Divisions"
             Title="Divisions">

    <ContentPage.BindingContext>
        <vm:ClubsViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" 
                HorizontalOptions="CenterAndExpand" >
            <Button Text="Atlantic" Command="{Binding GoTo}" CommandParameter="Atlantic"/>
            <Button Text="Central" Command="{Binding GoTo}" CommandParameter="Centeral"/>
            <Button Text="Southeast" Command="{Binding GoTo}" CommandParameter="Southeast"/>
            <Button Text="Northwest" Command="{Binding GoTo}" CommandParameter="Northwest"/>
            <Button Text="Pacific" Command="{Binding GoTo}" CommandParameter="Pacific"/>
            <Button Text="Southwest" Command="{Binding GoTo}" CommandParameter="Southwest"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>